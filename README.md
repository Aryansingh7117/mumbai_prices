# mumbai_prices
A complete machine learning regression project built in Python. Includes data cleaning, one-hot encoding, normalization, and model evaluation using MSE, RMSE, and R². Demonstrates the full ML workflow — from preprocessing to training and evaluation — using pandas, NumPy, and scikit-learn
